# 旅人教育 TABITO 网站文件

此目录是网站部署根目录。

当前仅保留一套现行前端资源：

- `index.html`：网站主体与各业务子页面
- `site.css`：现行视觉样式
- `site.js`：现行主页与导航逻辑
- `site-cleanup.js`：旧 HTML 内容兼容清理
- `preview.html`：staging 预览入口
- `images/`：网站图片资源

历史 `polish-v3` ～ `polish-v11`、hotfix、旧 preview 文件均已从当前工作树删除，不再参与运行。正式站点部署仍需单独执行，不由 staging 自动覆盖。